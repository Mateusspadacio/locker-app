describe('Example', () => {
  beforeEach(async () => {
    // await device.reloadReactNative();
  });

  it('should go to home component', async () => {
    await element(by.id('user_input')).typeText('my user silly hahahhahahahhahahahha');
  });
});
