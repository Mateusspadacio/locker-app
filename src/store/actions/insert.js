import axios from 'axios';
import { INSERT } from './actionTypes';

export const insert = () => {
    // some logic here
    return dispatch => {
        // some axios reques
        // dispatch(data) async operation
    }
}
